# Build dependencies (must be installed first)
setuptools>=69.0.3
wheel>=0.42.0
cython>=3.0.8

# HTTP and API
requests==2.31.0
httpx==0.27.0
aiohttp==3.9.3  # For async API client

# Web Scraping
beautifulsoup4==4.12.3
lxml==5.2.0
selenium==4.18.1  # For dynamic web scraping

# API Services
fastapi==0.95.2  # Using older version for Starlette compatibility with Prefect
starlette==0.27.0  # Explicitly pin Starlette version
uvicorn[standard]==0.27.1

# Database
sqlalchemy==2.0.27
psycopg2-binary==2.9.9
alembic==1.13.1  # For database migrations

# Data Processing
pandas==2.2.0
pyarrow==21.0.0  # For Parquet support
pyspark==3.5.0

# Orchestration Dependencies (order matters)
python-dateutil==2.8.2
croniter==2.0.3
pendulum>=3.0.0
prefect>=2.14.13  # Using newer version for pendulum 3.x compatibility
griffe>=0.38.1  # Required by newer prefect versions

# Testing and Development
pytest==8.0.1

# Configuration
pyyaml==6.0.1

# Type Checking
mypy==1.8.0  # For static type checking

# Type stubs for specific packages we use
types-requests==2.31.0.10
types-PyYAML==6.0.12.11
types-beautifulsoup4==4.12.0.7
pyarrow-stubs==21.0.0  # Type stubs matching our pyarrow version
