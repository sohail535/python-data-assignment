# Implement validation returning (valid, rejected).
from typing import Iterable, List, Tuple
from pipeline.models import Product

def validate(products: Iterable[Product]) -> Tuple[List[Product], List[Product]]:
    pass  # TODO
