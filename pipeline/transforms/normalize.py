# Implement field normalization (casing, rounding, clamping rating, currency uppercase).
from typing import Iterable, List
from pipeline.models import Product

def normalize(products: Iterable[Product]) -> List[Product]:
    pass  # TODO
